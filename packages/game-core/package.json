{
  "name": "@arena/game-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure-TS game engines, solvers, Elo and replay for the LLM Game Arena (no DOM/Node).",
  "exports": {
    ".": "./src/index.ts",
    "./package.json": "./package.json"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "4.1.10",
    "typescript": "^5.9.3",
    "vitest": "4.1.10"
  }
}
